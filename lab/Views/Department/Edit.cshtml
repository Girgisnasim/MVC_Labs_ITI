@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    SelectList list = new SelectList(ViewBag.poss, "SSN", "Fname");

}
@model Department
<h1>Add Department</h1>
<form asp-action="EditDB" asp-controller="Department">
    <input hidden asp-for="Dnum" />
    <div class="mb-3">
        <label asp-for="Dname" class="form-label"></label>
        <input type="text" class="form-control" asp-for="Dname">
    </div>

    <div class="mb-3">
        <label asp-for="MGRSSN" class="form-label"> Deparments</label>
        <select asp-for="MGRSSN" asp-items="list"></select>
        @*        <select class="form-select" aria-label="Default select example"asp-for="MGRSSN">
        @foreach (Employee item in ViewBag.poss)
        {
        <option value="@item.Manage.MGRSSN">@item.Fname @item.Lname</option>
        }
        </select> *@
    </div>
    <button type="submit" class="btn btn-primary">Add</button>
</form>